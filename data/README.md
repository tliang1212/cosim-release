Place a local prototype copy of the release-plan Excel file here as:

```text
cosim_release_plan.xlsx
```

Do not commit customer or internal release-plan spreadsheets. For a shared
Databricks App, use a governed Unity Catalog Volume or DBFS path and set
`COSIM_EXCEL_PATH` in `app.yaml`.
